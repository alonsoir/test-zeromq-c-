cd /vagrant

# Stage changes
git add crypto-transport/src/compression.cpp
git add etcd-server/src/etcd_server.cpp

# Commit
git commit -m "fix(crypto): Add 4-byte header to LZ4 compression format

BREAKING CHANGE: Compression format now includes 4-byte big-endian header

- crypto-transport: compress() now prepends [size_byte0-3][data]
- etcd-server: decompress extracts header before LZ4 decompress
- Fixes 'Invalid decompressed size' errors (4GB+ phantom sizes)

Tested:
- 200 events: 0 errors, 100% success
- 20K events: decompression stable, ipset failures under investigation

Components requiring verification:
- ml-detector, sniffer, RAG (if using compression)
- All stress test scripts

Related: Day 51 compression header bug investigation"

# Push
git push origin main