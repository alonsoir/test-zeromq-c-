git add sniffer/src/userspace/ring_consumer.cpp \
        sniffer/src/userspace/ml_defender_features.cpp

git commit -m "fix(sniffer): DAY 77 — NaN sentinels + correct populate order

Replace 0.5f sentinels with quiet_NaN() across the feature pipeline.

Problems fixed:
- init_embedded_sentinels() used 0.5f which could coincide with real
  traffic (e.g. tcp_udp_ratio≈0.5), causing model to interpret missing
  data as valid signal
- populate_protobuf_event() called init_embedded_sentinels() AFTER
  populate_ml_defender_features(), overwriting all real values with
  sentinels on every event (bug present since day 1, masked by DAY 76
  sentinel overwrite fix)
- 12 Phase 2 extractors returned 0.5f instead of NaN, indistinguishable
  from real measurements

Changes:
- ring_consumer.cpp: upgrade init_embedded_sentinels() to quiet_NaN()
  for all 40 fields (4 submessages)
- ring_consumer.cpp: fix order in populate_protobuf_event():
  NaN sentinel → populate_ml_defender_features() → run_ml_detection()
- ml_defender_features.cpp: 12 TODO(Phase 2) extractors now return
  quiet_NaN() instead of 0.5f, pending TimeWindowAggregator injection

Semantics: NaN = 'not computed / Phase 2 pending'
           real value = extracted from FlowStatistics
           no field ever looks like valid data when it is not

Real features now flowing: 28/40 (ddos 10/10, ransomware 10/10,
traffic 3/10, internal 5/10). Remaining 12 require TimeWindowAggregator
injection — planned DAY 78.

Tests: ml-detector 9/9, crypto-transport 3/3, etcd-client 12/12"