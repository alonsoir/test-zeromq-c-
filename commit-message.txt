git add ml-detector/tests/CMakeLists.txt \
        ml-detector/tests/integration/test_csv_event_writer.cpp \
        ml-detector/tests/integration/test_csv_feature_extraction.cpp \
        ml-detector/tests/integration/test_etcd_client_hmac.cpp \
        ml-detector/src/etcd_client.cpp \
        ml-detector/CMakeLists.txt

git commit -m "day64: recreate test suite + CSV pipeline tests + etcd_client patch

- tests/CMakeLists.txt: recreated (was lost); covers all unit/ and
  integration/ targets; test_detectors excluded from ctest (standalone
  benchmark); test_ransomware_detector_integration disabled (AND FALSE,
  preexisting namespace errors)

- tests/integration/test_csv_event_writer.cpp: 127-column schema
  validation, HMAC-SHA256 verification, score threshold filtering,
  file rotation, zero-fill for absent proto sub-messages, concurrency
  smoke test

- tests/integration/test_csv_feature_extraction.cpp: living contract
  between network_security.proto and FEATURE_SCHEMA.md; verifies all
  proto field accessors compile, section column counts, reproducibility,
  numeric parseability

- tests/integration/test_etcd_client_hmac.cpp: mock httplib server
  tests for get_hmac_key() — happy path, 404/500 error paths, malformed
  JSON, connection refused; uses real constructor EtcdClient(endpoint,
  component); pending: /usr/local/include in CMakeLists for httplib.h

- src/etcd_client.cpp: move get_hmac_key() inside struct Impl (was
  accidentally placed as a free function); use client_->get_hmac_key()
  from etcd-client API (vector<uint8_t> -> hex string); add sstream +
  iomanip includes; fix missing }; closing Impl

- ml-detector/CMakeLists.txt: replace inline BUILD_TESTS blocks with
  add_subdirectory(tests)

Compiles clean: ml-detector, test_csv_feature_extraction,
test_csv_event_writer. test_etcd_client_hmac pending include path fix.

Co-authored-by: Claude (Anthropic)"