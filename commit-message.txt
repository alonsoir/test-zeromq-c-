---

### Commit para hoy

```
Day 66: hardcoded audit 34/34, CSV end-to-end verified (127 cols, HMAC ok)

- config: csv_writer + scoring sections, DetectorConfig structs + loader
- zmq_handler: CsvEventWriter standalone, decoupled from RAG Logger
- zmq_handler: all thresholds/paths from JSON (no magic numbers)
- synthetic_injector: --attack mode (DDoS signature + forced fast score)
- synthetic_injector: remove dead general_attack_features loop
- etcd_client: revert compression_enabled=false (broke config upload)

CSV pipeline: 49 events written, 127 cols, HMAC verified
Next: --ransomware mode, RAG Logger diagnosis, rag-ingester Ruta A
```
