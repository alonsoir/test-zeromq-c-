cd /Users/aironman/CLionProjects/test-zeromq-docker
git add ml-detector/src/etcd_client.cpp \
        sniffer/src/userspace/etcd_client.cpp \
        firewall-acl-agent/src/core/etcd_client.cpp \
        etcd-server/src/component_registry.cpp
git commit -m "fix(crypto): set component_config_path in etcd_client adapters — DAY 107

Root cause of MAC verification failed resolved.
component_config_path was not set in etcd_client::Config across three
components, leaving tx_ null. Silent guard in process_outgoing_data()
skipped encryption entirely, sending LZ4-compressed plaintext as
application/octet-stream. Server attempted ChaCha20-Poly1305 decrypt
on raw LZ4 — MAC failure guaranteed.

Fixes:
- ml-detector/src/etcd_client.cpp: add component_config_path
- sniffer/src/userspace/etcd_client.cpp: add component_config_path
  + rewrite get_encryption_seed() to read local seed.bin
- firewall-acl-agent/src/core/etcd_client.cpp: add component_config_path
- etcd-server/src/component_registry.cpp: swap CTX_ETCD_TX/RX
  (rx_ uses TX to decrypt client requests, tx_ uses RX for responses)
  NOTE: swap needs independent verification in DAY 108 PASO 1

Root cause identified via hexdump of first 16 bytes at server:
  7b 0a 20 20 22 5f 68 65 61 64 = { newline  \"_head
  JSON visible in what should have been ciphertext.

VM-only fixes (not yet in provision.sh — DAY 108 PASO 3):
- chmod 755 /etc/ml-defender/ component dirs
- chmod 640 + chown root:vagrant seed.bin (all 6 components)
- Seeds synchronized (etcd-server seed.bin copied to all)
- JSON symlinks /etc/ml-defender/*/ -> /vagrant/*/config/
- libsodium.so.23 -> libsodium.so.26 symlink
- libcrypto_transport.so rebuilt (was Feb 16, now Apr 4)

Diagnosed with Consejo de Sabios (7 LLM reviewers).
Hexdump technique suggested by Gemini.

Closes: MAC verification failed on PUT /v1/config/{component}
Pending: provision.sh formalization (DAY 108 PASO 3)
Pending: fail-fast invariant if tx_ null when encryption enabled
Pending: verify CTX swap necessity independently (DAY 108 PASO 1)

Co-authored-by: Alonso Isidoro Roman <alonsoir@gmail.com>
Co-authored-by: Claude (Anthropic) <claude@anthropic.com>"