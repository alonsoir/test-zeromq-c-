feat(sniffer): DAY 78 — MISSING_FEATURE_SENTINEL + TimeWindowAggregator + 9 multi-flow extractors + run_ml_detection scores→proto

- Define MISSING_FEATURE_SENTINEL=-9999.0f (mathematically unreachable
  by any embedded tree split, range [0.0,5.1]); replaces quiet_NaN()
  (40 sentinels in ring_consumer.cpp) and 12 Phase-2 TODOs in
  ml_defender_features.cpp
- Add TimeWindowAggregator as member of RansomwareFeatureProcessor;
  expose get_aggregator(); feed tw_event on every process_packet()
- Inject aggregator lazily into thread_local MLDefenderExtractor via
  set_aggregator()/has_aggregator() before populate_ml_defender_features()
- Implement 9 multi-flow extractors using WindowStats (event_count,
  unique_ips_count, unique_ports_count): connection_rate,
  src_ip_entropy, dst_ip_concentration, port_entropy,
  internal_connection_rate, lateral_movement_score,
  service_port_consistency, service_discovery_patterns,
  ddos_source_ip_dispersion → 29/40 real features (was 28/40)
- Implement run_ml_detection PHASE 3/4: write overall_threat_score,
  final_classification, threat_category, ml_analysis fields to proto;
  sentinel -1.0 for non-inferred scores
- Tests: crypto 3/3, etcd-hmac 12/12, ml-detector 9/9,
  trace_id 44/46 (2 pre-existing DAY72 failures)