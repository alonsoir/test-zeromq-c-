git add -A
git commit -m "DAY 75: Proto3 embedded serialization bug fix + regression tests

ROOT CAUSE IDENTIFIED & PARTIALLY FIXED:
- ring_consumer.cpp::send_ransomware_features() never called mutable_ddos_embedded(),
  mutable_traffic_classification(), mutable_internal_anomaly() → submessages absent
  on wire → ml-detector crashes in ByteSizeLong() dereferencing null this=0x0

FIXES APPLIED:
- ring_consumer.cpp: Populate 3 missing embedded messages with Phase 1 sentinel
  values (0.5f) to force proto3 serialization (DAY 75 FIX @ line 1190)
- rag_logger.cpp: Remove incorrect proto2-style has_required_embedded guard
  (DAY 75 FIX @ line 393) — proto3 submessages never return null pointers
- rag_logger.cpp: Add crypto_manager_ null guard before artifact save
- zmq_handler.cpp: Add null guards for crypto_manager_ and output_socket_
  in send_enriched_event() for defensive diagnostics

REGRESSION TESTS (6/6 PASSING):
- sniffer/tests/test_proto3_embedded_serialization.cpp (3 tests)
  * NeverCallingMutableMeansSubmessageAbsentOnWire
  * SentinelValuesForceSubmessageSerialization
  * NonZeroValuesFromRealFlowAlwaysSerialize
- ml-detector/tests/unit/test_rag_logger_artifact_save.cpp (3 tests)
  * AccessingUnsetSubmessageIsAlwaysSafeInProto3
  * EventWithSentinelsPassesEmbeddedCheck
  * NormalFlowEventsUnaffectedByFix

PENDING:
- sniffer build-debug needs clean rebuild (cmake fails: libsnappy missing + unknown
  second error). ring_consumer.cpp fix is in source but not yet in running binary.
  ml-detector continues crashing until sniffer is rebuilt.

Makefile: Decouple *-start targets from compilation (pipeline-start no longer
triggers full rebuild of all components)

Co-authored-by: Claude Sonnet (Anthropic)
Co-authored-by: [Consejo de Sabios pending review]"