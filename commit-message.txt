cd /vagrant && git add -A && git commit -m "Day 73: Fix etcd/rag startup bugs, Makefile pipeline stable

- etcd_client.cpp: add register_component() before put_config() in registerService()
- etcd_client.cpp: set compression_min_size=0 to force LZ4 always (fix 500 error)
- main.cpp (rag): add isatty() daemon mode with sigwait() - no more infinite loop
- main.cpp (rag): add cin.eof() guard for interactive mode
- etcd-server/main.cpp: add exit(0) to signal_handler
- Makefile: pipeline-start/stop/status working for etcd-server + rag-security
- Makefile: fix pkill via vagrant ssh using ps+grep pattern instead
- Makefile: nohup with sleep inside same SSH session (keeps session alive)
- Makefile: add etcd-server-status, rag-status, rag-logs targets
- rag-config.json: add logging section with path"