git add -A
git commit -m "feat(day-62): Contextual HTTP logging + heartbeat path fix

Fixes:
- etcd-client: heartbeat path /heartbeat -> /v1/heartbeat/{component_name}

Logging (etcd-client/http_client.cpp):
- [HTTP→] before each request: method, host:port, path, ts ISO8601, component
- [HTTP←] after response: status, path, duration_us, component
- X-Component-Name and X-Request-Timestamp headers on all requests
- Helper iso8601_now() and duration_us()

Logging (etcd-server/etcd_server.cpp):
- [ETCD←] on every handler: method, path, status, component, client_ts, server_ts
- Helper server_iso8601_now() and log_request()
- Fixed /*req*/ -> req in handlers that now need the request object

Result:
- Full client<->server correlation via timestamps (typically 1-2ms delta)
- component_name visible on both sides of every HTTP exchange

Debt noted:
- status: active hardcoded in heartbeat payload
- Duplicate component.name registration not rejected by etcd-server
- etcd-server does not start from Makefile
- Per-component etcd_client config (currently shared)

Co-authored-by: Claude (Anthropic)
Piano piano 🏛️"