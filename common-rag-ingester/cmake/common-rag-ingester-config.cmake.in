@PACKAGE_INIT@

include(CMakeFindDependencyMacro)

# Find FAISS dependency
find_dependency(PkgConfig REQUIRED)
pkg_check_modules(FAISS REQUIRED faiss)

# Include targets
include("${CMAKE_CURRENT_LIST_DIR}/common-rag-ingester-targets.cmake")

check_required_components(common-rag-ingester)
