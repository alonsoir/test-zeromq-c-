📝 Notas del Consejo de Sabios — DAY 141 (8/8)

"DAY 141 — Bug Makefile seed-client-build cerrado. DEBT-PCAP-CALLBACK-LIFETIME-DOC-001 cerrado. DEBT-VARIANT-B-CONFIG-001 cerrado — sniffer-libpcap.json propio + main_libpcap.cpp config-driven. 9/9 tests PASSED. 0 warnings. Emails FEDER enviados a Andrés Caro Lindo.
Q1 (8/8 + founder): Identidad criptográfica compartida component_id="sniffer" correcta — Variant A y Variant B son la misma identidad con distinto backend. PERO: exclusión mutua obligatoria. DEBT-VARIANT-B-MUTEX-001 registrada. Nivel 1 (advertir + stop ambos si detectan coexistencia) via script bash/python en Makefile, pre-FEDER. Nivel 2 (hardware-aware kill: Variant A verifica eBPF disponible, Variant B verifica ARM64 o ausencia de eBPF) post-FEDER — con Nivel 1 implementado, la mitad del trabajo estará hecha. La lógica de detección NO entra en los binarios. AppArmor: mismo perfil para ambas variantes — mismos permisos de red, sin root. La distinción la hace etcd al registrarse.
Q2 (8/8 + founder): buffer_size_mb pre-FEDER obligatorio. Variable por diseño — el script de barrido paramétrico necesita modificar el valor entre ejecuciones para trazar la curva de optimización. Primero en VM/macOS para la forma de la curva, luego en hardware real para los valores definitivos publicables. Sin curva empírica, los resultados ARM64/RPi en el paper estarán contaminados por una limitación artificial del software. DEBT-VARIANT-B-BUFFER-SIZE-001 elevada a P1 pre-FEDER.
Q3 (8/8 + founder): Script grep/awk determinista para clasificar warnings de build. Un LLM no determinista no debe hacer trabajo determinista. BACKLOG-BUILD-WARNING-CLASSIFIER-001 actualizado: implementar como script grep/awk, no como clasificador TinyLlama. Patrones conocidos: prefijo defender:, paths /tmp/faiss/, libtool:.
Q4 (8/8): EMECAS es suficiente gate ahora. make check-deps post-FEDER cuando exista Jenkins — un make clean && make all en VM limpia en cada PR es equivalente y cubre el gap.
'buffer_size_mb no es una opción de confort — es una variable experimental. Sin ella, el benchmark ARM64 mide el default del kernel, no el hardware.' — Claude
'La exclusión mutua implementada en el script de arranque, no en el binario, es la decisión correcta: separación de responsabilidades, sin añadir lógica de orquestación a un componente de captura.' — Grok"
— Consejo de Sabios (8/8) · DAY 141

